name: Sync Threat Database

on:
  schedule:
    - cron: '0 6 * * *' # Daily at 06:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  sync-advisories:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Sync npm security advisories
        id: sync
        run: |
          bun run scripts/sync-advisories.ts | tee sync-output.txt
          echo "summary=$(tail -1 sync-output.txt)" >> "$GITHUB_OUTPUT"
        continue-on-error: true

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet packages/shared/data/seed-threats.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Create Pull Request
        if: steps.changes.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: 'chore: update threat database with new advisories'
          title: 'chore: update threat database (${{ steps.sync.outputs.summary }})'
          body: |
            Automated threat database update from GitHub Advisory Database.

            ${{ steps.sync.outputs.summary }}

            This PR was auto-generated by the daily advisory sync workflow.
          branch: chore/sync-threat-db
          labels: threat-db
          delete-branch: true
