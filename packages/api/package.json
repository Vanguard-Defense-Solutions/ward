{
  "name": "@ward/api",
  "version": "0.2.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@ward/shared": "workspace:*"
  }
}
